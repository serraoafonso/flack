using UnityEngine;

public class CollisionFunction : MonoBehaviour
{
    // Referência ao Canvas que será ativado

    public string bookID;

    public string tema;


    private void Start()
    {
        // Obtém a referência ao QuizManager no Canvas

       
    }

    // Método que é chamado quando o jogador colide com o objeto (Trigger)
    
    
}
